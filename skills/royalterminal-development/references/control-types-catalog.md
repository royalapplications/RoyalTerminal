# Control Types Catalog

## Public Avalonia Controls

### `TerminalControl`

File:

- `src/RoyalTerminal.Avalonia/Controls/TerminalControl.cs`

Purpose:

- cross-platform terminal UI
- works with managed VT, native Ghostty VT, and auto VT selection
- owns session routing, rendering, input, selection, capture, and replay integration

## Removed Control Package

The old macOS-only embedded Ghostty control package was removed from the product surface.

Do not add new product code against deleted embedded-control APIs.
