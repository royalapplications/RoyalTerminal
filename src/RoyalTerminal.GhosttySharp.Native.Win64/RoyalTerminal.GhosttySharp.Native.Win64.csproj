<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>RoyalTerminal.GhosttySharp.Native.Win64</PackageId>
    <Description>Native Ghostty libraries for Windows x64. Provides ghostty.dll, ghostty-vt.dll, ghostty-terminal.dll, and ghostty-renderer-capi.dll shared libraries required by RoyalTerminal.GhosttySharp on Windows.</Description>
    <PackageTags>ghostty;terminal;native;windows;x64</PackageTags>

    <!-- This is a native-only package, no managed assembly -->
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
    <NoWarn>NU5128</NoWarn>
  </PropertyGroup>

  <!-- NuGet pack: include all runtimes -->
  <ItemGroup>
    <None Include="runtimes/win-x64/native/**" Pack="true" PackagePath="runtimes/win-x64/native" Condition="Exists('runtimes/win-x64/native')" />
  </ItemGroup>

  <ItemGroup>
    <None Include="build/RoyalTerminal.GhosttySharp.Native.Win64.targets" Pack="true" PackagePath="build" />
    <None Include="buildTransitive/RoyalTerminal.GhosttySharp.Native.Win64.targets" Pack="true" PackagePath="buildTransitive" />
  </ItemGroup>

</Project>
